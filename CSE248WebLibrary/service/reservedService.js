const mongoose = require("mongoose");


function findReservedByUserId(userId, callback) {
        callback(reserved);
}

function addReserved(reserved, callback) {
        callback(reserved);

}

function deleteReservedById(id, callback) {
        callback(reserved);
}

function findAllReserveds(callback) {
        callback(reserved);
}
